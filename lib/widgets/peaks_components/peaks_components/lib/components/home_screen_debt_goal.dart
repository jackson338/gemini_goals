import 'package:flutter/material.dart';

class HomeScreenDebtComponent extends StatelessWidget {
  const HomeScreenDebtComponent({super.key});
  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(
        borderRadius: BorderRadius.circular(8),
        color: Colors.white,
      ),
    );
  }
}
